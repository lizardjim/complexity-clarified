<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">

    <a class="navbar-brand d-flex align-items-center gap-2 fw-semibold" href="/">
      <img
        src="/cc-logo-v2.png"
        alt="Complexity Clarified"
        width="32"
        height="32"
      />
      <span>Complexity Clarified</span>
    </a>

    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
      aria-controls="navbarNav"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">

      <!-- Normal navigation -->
      <div
        class="navbar-nav-wrapper ms-auto d-lg-flex align-items-lg-center"
        id="navbarLinks"
      >
        <ul class="navbar-nav gap-lg-2 align-items-lg-center">

          <li class="nav-item">
            <a class="nav-link nav-small" href="/approach">My Approach</a>
          </li>
          
          <li class="nav-item">
            <a class="nav-link nav-small" href="/decisions">Decisions</a>
          </li>

          <li class="nav-item">
            <a class="nav-link nav-small" href="/leadership">Leadership</a>
          </li>

          <li class="nav-item">
            <a class="nav-link nav-small" href="/technology">Technology</a>
          </li>

          <li class="nav-item">
            <a class="nav-link nav-small" href="/change">Change</a>
          </li>

          <li class="nav-item">
            <a class="nav-link nav-small" href="/about">About</a>
          </li>

          <li class="nav-item">
            <a href="/contact" class="btn btn-outline-light rounded-4 fw-semibold px-3 ms-lg-2">Let's Talk</a>
          </li>

          <!-- Desktop search trigger -->
          <li class="nav-item d-none d-lg-flex">
            <button
              class="nav-link nav-small nav-search-trigger"
              type="button"
              id="openNavSearch"
              aria-label="Search Complexity Clarified"
              title="Search"
            >
              <svg
                xmlns="http://www.w3.org/2000/svg"
                width="20"
                height="20"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
                stroke-linecap="round"
                stroke-linejoin="round"
                aria-hidden="true"
              >
                <circle cx="11" cy="11" r="8"></circle>
                <path d="m21 21-4.35-4.35"></path>
              </svg>
            </button>
          </li>

          <!-- Mobile search -->
          <li class="nav-item d-lg-none mt-2 mb-2">
            <form
              class="mobile-nav-search"
              action="/search/"
              method="get"
              role="search"
            >
              <label
                class="visually-hidden"
                for="mobileNavSearch"
              >
                Search Complexity Clarified
              </label>

              <div class="input-group">

                <input
                  id="mobileNavSearch"
                  class="form-control"
                  type="search"
                  name="q"
                  placeholder="Search Complexity Clarified"
                  aria-label="Search Complexity Clarified"
                />

                <button
                  class="btn btn-outline-light"
                  type="submit"
                  aria-label="Search"
                >
                  <svg
                    xmlns="http://www.w3.org/2000/svg"
                    width="18"
                    height="18"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    aria-hidden="true"
                  >
                    <circle cx="11" cy="11" r="8"></circle>
                    <path d="m21 21-4.35-4.35"></path>
                  </svg>
                </button>

              </div>
            </form>
          </li>

        </ul>
      </div>

      <!-- Desktop expanded search -->
      <form
        class="nav-search-expanded ms-auto d-none align-items-center"
        id="navSearchForm"
        action="/search/"
        method="get"
        role="search"
      >

        <label
          class="visually-hidden"
          for="desktopNavSearch"
        >
          Search Complexity Clarified
        </label>

        <input
          id="desktopNavSearch"
          class="form-control nav-search-input"
          type="search"
          name="q"
          placeholder="Search Complexity Clarified..."
          autocomplete="off"
          aria-label="Search Complexity Clarified"
        />

        <button
          class="nav-search-submit"
          type="submit"
          aria-label="Search"
          title="Search"
        >
          <svg
            xmlns="http://www.w3.org/2000/svg"
            width="20"
            height="20"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
            stroke-linecap="round"
            stroke-linejoin="round"
            aria-hidden="true"
          >
            <circle cx="11" cy="11" r="8"></circle>
            <path d="m21 21-4.35-4.35"></path>
          </svg>
        </button>

        <button
          class="nav-search-close"
          type="button"
          id="closeNavSearch"
          aria-label="Close search"
          title="Close search"
        >
          &times;
        </button>

      </form>

    </div>

  </div>
</nav>

<script>
  const openSearch = document.getElementById('openNavSearch');
  const closeSearch = document.getElementById('closeNavSearch');
  const navbarLinks = document.getElementById('navbarLinks');
  const searchForm = document.getElementById('navSearchForm');

  const searchInput =
    document.getElementById('desktopNavSearch') as HTMLInputElement | null;

  function showSearch() {
    if (!navbarLinks || !searchForm || !searchInput) return;

    navbarLinks.classList.add('d-lg-none');

    searchForm.classList.remove('d-none');
    searchForm.classList.add('d-lg-flex');

    searchInput.focus();
  }

  function hideSearch() {
    if (!navbarLinks || !searchForm || !searchInput) return;

    searchForm.classList.add('d-none');
    searchForm.classList.remove('d-lg-flex');

    navbarLinks.classList.remove('d-lg-none');

    searchInput.value = '';
  }

  openSearch?.addEventListener('click', showSearch);
  closeSearch?.addEventListener('click', hideSearch);

  document.addEventListener('keydown', (event) => {
    if (event.key === 'Escape') {
      hideSearch();
    }
  });
</script>